import { createTheme } from "@mui/material";

export const DARK_THEME = createTheme({
  palette: {
    mode: "dark",
  },
});

export const COLORS = {
  POSITIVE: "#127a02",
  NEGATIVE: "#8c0410",
};
